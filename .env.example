# Copy to `.env` for local dev (`.env` is git-ignored).

# Base URL of the ArtisanAI voice microservice (see ai-service/).
# Only needed if it isn't on the default below.
VITE_AI_SERVICE_URL=http://localhost:8000
